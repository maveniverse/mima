name: CI

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    name: Verify
    uses: maveniverse/parent/.github/workflows/ci.yml@main
    with:
      maven-test: './mvnw -f demo install -e -B -V'
